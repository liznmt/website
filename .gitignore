# lizzys-lab repo is REFERENCE ONLY — separate repo, its own Netlify deploy.
# Never commit its contents here. Copy specific files deliberately if needed.
lizzyslab/
lizzys-lab/

# raw media library stays local — only optimized derivatives inside site/ are committed
# (anchored to repo root so site/press/ etc. stay committable)
/photos/
/press/
*.mov
*.MOV
*.mp4
*.MP4

# EXCEPTION: deployed scroll-scrub clips live here (see MOTION.md video spec)
!site/assets/video/
!site/assets/video/*.mp4

.DS_Store
